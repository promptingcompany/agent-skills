# Generative Engine Optimization (GEO)

Agent simulation and GEO simulation prompt generation for AI visibility auditing.

## Workflows

| Workflow | Triggers |
|---|---|
| Agent simulation | "simulate agent", "run agent loop", "test agent" |
| GEO simulation prompts | "generate prompts for [product]", "create GEO audit prompts", "build a prompt bank" |
| Open a PR | "open a PR", "submit my changes", "push this skill", "create a pull request" |

## Install

```bash
cp -r skills/generative-engine-optimization ~/.claude/skills/
```

See [`INSTALL.md`](INSTALL.md) for claude.ai and MCP server setup.
